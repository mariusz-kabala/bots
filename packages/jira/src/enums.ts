export enum ISSUE_STATUS {
  backlog = 'Backlog',
  inProgress = 'In progress',
  done = 'Done',
  closed = 'Closed',
}

export enum ISSUE_TYPES {
  bug = 'bug',
  story = 'story',
  epic = 'epic',
  task = 'task',
  subtask = 'subtask',
}

export enum TIME_FIELDS {
  updated = 'updated',
  created = 'created',
}
